{mktype_template}
{setnormalinvert_template}
                <drawfile{file_type} file="{file}" objname="{label}" autofill="{autofill}" advanced="{adm_enabled}" reverse="{adm_reverse_enabled}" >
                    {depth}
                    <drawscale x="{scale_x}" y="{scale_y}" z="{scale_z}" />
                    <drawmove x="{move_x}" y="{move_y}" z="{move_z}" />
                    <drawrotate angx="{rot_x}" angy="{rot_y}" angz="{rot_z}" />
                </drawfile{file_type}>