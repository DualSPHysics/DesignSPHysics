{move_template}
{rotation_template}
{mktype_template}
                <setdrawmode mode="{obj[fillmode]}"/>
                <drawbox objname="{label}">
                    <boxfill>{boxfill}</boxfill>
                    <point x="{pos[0]}" y="{pos[1]}" z="{pos[2]}" />
                    <size x="{size[0]}" y="{size[1]}" z="{size[2]}" />
                </drawbox>
{matrixreset_template}