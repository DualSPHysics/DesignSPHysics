{move_template}
{rotation_template}
{mktype_template}
                <setdrawmode mode="{obj[fillmode]}"/>
                <drawcylinder radius="{radius}" objname="{label}">
                    <point x="{pos[0]}" y="{pos[1]}" z="{pos[2]}" />
                    <point x="{pos[0]}" y="{pos[1]}" z="{z2}" />
                </drawcylinder>
{matrixreset_template}