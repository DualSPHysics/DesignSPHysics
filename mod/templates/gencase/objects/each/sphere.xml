{move_template}
{rotation_template}
{mktype_template}
                <setdrawmode mode="{obj[fillmode]}"/>
                <drawsphere radius="{radius}" objname="{label}">
                    <point x="{pos[0]}" y="{pos[1]}" z="{pos[2]}" />
                </drawsphere>
{matrixreset_template}